<%- include('header', {title: title }); -%>
  <body>
    <%- include('nav'); -%> 
      <% if(locals.inputErr===true){ %>       
        <div class="alert alert-danger alert-dismissible fade show"> 
          <button type="button" class="close" data-dismiss="alert">&times;</button>
          <strong>Invalid evidence!</strong> <%=err %>
        </div>
      <%   } %> 
    <div class='container col-10 bg-light border mt-4'>
      <div class="">
      <form action=<%=action %> method="POST" >
        <%- include('form-note', {date:locals.date, time:locals.time, behavior:locals.behavior, flock_size:locals.flock_size}); -%>
      </form>
      </div>
  </body>
</html>


